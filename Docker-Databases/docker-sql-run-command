Create Docker Container :- 

docker run -e "ACCEPT_EULA=1" -e "MSSQL_SA_PASSWORD=Admin260" -e "MSSQL_PID=Developer" -p 1433:1433 -d --name SqlServer --restart always mcr.microsoft.com/azure-sql-edge


++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Copy Backup to Container:-
Terminal Command to move Adventure Works to SQL Docker Container : 


ON TERMINAL:-
docker cp /Users/praveen/Downloads/AdventureWorksDW2019.bak SqlServer:/var/opt/mssql/data/


ON AZURE DATA STUDIO:

-- Step 1: Verify the logical file names from the backup (optional, if not already done)
RESTORE FILELISTONLY 
FROM DISK = '/var/opt/mssql/data/AdventureWorksDW2019.bak';
GO

-- Step 2: Restore the database using the correct logical file names
RESTORE DATABASE AdventureWorksDW2019
FROM DISK = '/var/opt/mssql/data/AdventureWorksDW2019.bak'
WITH MOVE 'AdventureWorksDW2019' TO '/var/opt/mssql/data/AdventureWorksDW2019.mdf',  -- Correct logical name for the data file
MOVE 'AdventureWorksDW2019_log' TO '/var/opt/mssql/data/AdventureWorksDW2019.ldf',  -- Correct logical name for the log file
REPLACE;  -- Overwrite the database if it already exists
GO




